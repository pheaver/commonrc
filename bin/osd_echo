#!/bin/bash

notify "" "$@"

#if [[ `uname` == Darwin ]]; then
#    echo $@ | growlnotify $title 2>/dev/null
#    say $@
#else
#    echo "Error: this computer ($HOSTNAME) should not be running osd_echo"
#fi
